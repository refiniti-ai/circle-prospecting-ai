import { FlyerProductSheet } from "./FlyerProductSheet";

/** /campaign-pricing — agent sales product sheet (print PDF layout). */
export function CampaignPricingRedesign() {
  return <FlyerProductSheet />;
}
