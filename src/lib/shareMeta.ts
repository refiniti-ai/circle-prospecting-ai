import { PRODUCTION_SITE_ORIGIN } from "./siteUrl";

/** Link preview — keep in sync with index.html (crawlers read static HTML only). */
export const SHARE_IMAGE_URL = `${PRODUCTION_SITE_ORIGIN}/preview-image.jpg`;
