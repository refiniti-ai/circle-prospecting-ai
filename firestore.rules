rules_version = '2';
// Tight default: adjust per collection before using the client SDK in the browser.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
