# Production build: use same-origin `/api/*` — firebase.json rewrites `/api/**` to Cloud Run (no CORS in the browser).
# Overrides a local `.env` VITE_API_BASE_URL for `npm run build` / `vite build` (mode production).
VITE_API_BASE_URL=
