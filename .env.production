# Loaded only for `vite build` — public API origin (no secrets).
# Keeps production SPA calls working when local .env is not present (CI, other machines).
VITE_API_BASE_URL=https://circle-prospecting-ai-git-724527267367.us-central1.run.app
