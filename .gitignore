node_modules
dist
dist-aws-staging
.firebase
report.zip
temp-ref-*.jpg
.design-ref
.DS_Store
*.local
.env
.env.local
.env.*.local

# Credentials — never commit
secrets/
*serviceAccountKey*.json
*.pem
*.p12

# Runtime data written by the API (customer, order and payment records)
server/data/*.json
server/data/platform/
!server/data/.gitkeep

# Generated AWS config (mirrors live service settings)
infra/aws/apprunner-env.generated.json
infra/aws/apprunner-service.generated.json

# Local scratch files and third-party data dumps
_*.json
tmp-*.json

# Test payloads containing real contact details
docs/email-templates/ghl-webhook-test-payload.json
docs/ghl-workflow-test-*.json

# Extracted copy of a previous deployment — not source (141 MB)
deploy-recovery/
