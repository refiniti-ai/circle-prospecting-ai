rules_version = '2';
// Tight default: open paths (e.g. users/{uid}/...) when you add uploads.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
